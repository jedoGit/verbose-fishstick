apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pv-db-claim
  namespace: ns
spec:
  storageClassName: manual
  volumeName: pv-db
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
